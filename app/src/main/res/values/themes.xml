<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobAlert" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/alerts_day</item>
        <item name="colorSecondary">@color/navbar</item>
        <item name="android:windowBackground">@color/alerts_day</item>
        <item name="materialCardViewStyle">@style/CustomMaterialCardView</item>
        <item name="floatingActionButtonStyle">@style/CustomFloatingActionButton</item>

    </style>

    <style name="Theme.MobAlert" parent="Base.Theme.MobAlert" />
</resources>